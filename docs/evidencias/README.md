# Evidencias para memoria TFG

Guarda aqui:
- capturas de `npm test` (api-gateway, microservicioUsuarios)
- capturas de `npm run coverage` (api-gateway, microservicioUsuarios)
- capturas de `python -m pytest`
- capturas de `python -m pytest --cov=app`
- capturas de HTML coverage
- capturas de Postman
- capturas de Docker (`docker compose ps`, health checks)
